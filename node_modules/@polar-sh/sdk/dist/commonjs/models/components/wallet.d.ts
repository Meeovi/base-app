import * as z from "zod/v4-mini";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
/**
 * A wallet represents a customer's balance in your organization.
 *
 * @remarks
 *
 * They can top-up their wallet, and use the balance to pay for usage.
 */
export type Wallet = {
    /**
     * The ID of the object.
     */
    id: string;
    /**
     * Creation timestamp of the object.
     */
    createdAt: Date;
    /**
     * Last modification timestamp of the object.
     */
    modifiedAt: Date | null;
    /**
     * The ID of the customer that owns the wallet.
     */
    customerId: string;
    /**
     * The current balance of the wallet, in cents.
     */
    balance: number;
    /**
     * The currency of the wallet.
     */
    currency: string;
};
/** @internal */
export declare const Wallet$inboundSchema: z.ZodMiniType<Wallet, unknown>;
export declare function walletFromJSON(jsonString: string): SafeParseResult<Wallet, SDKValidationError>;
//# sourceMappingURL=wallet.d.ts.map