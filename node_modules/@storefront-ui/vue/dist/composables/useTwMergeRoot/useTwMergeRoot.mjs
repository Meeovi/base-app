import { twMerge as u } from "tailwind-merge";
import { useAttrs as f, computed as i } from "vue";
const y = () => {
  const t = f();
  return {
    attrsWithoutClass: i(() => {
      const { class: r, ...e } = t;
      return e;
    })
  };
}, o = (t, r) => {
  if (!r || typeof r == "boolean") return t;
  if (Array.isArray(r))
    return r.reduce(o, t);
  if (typeof r == "object") {
    let e = "";
    for (const [n, s] of Object.entries(r))
      s && (e += ` ${n}`);
    return t += e;
  }
  return t += ` ${r}`;
}, b = (...t) => u(t.reduce(o, ""));
export {
  b as twMerge,
  y as useTwMergeRoot
};
